import {
  
} from './actions';

// TODO: To get a better understand of how a reducer works - add comments to the various actions in the reducer
// The reducer is a function that takes our current state and action and returns a new state based on that action

// export const reducer = (state, action) => {
//   switch (action.type) {
// };